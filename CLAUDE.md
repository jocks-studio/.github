@AGENTS.md

<!--
Claude Code reads CLAUDE.md, not AGENTS.md. The import above pulls in the shared
agent instructions so every tool reads one file. Put changes in AGENTS.md; add
Claude-specific instructions below the import only if they apply to Claude Code
alone. HTML comments like this one are stripped before the file enters context.
-->
